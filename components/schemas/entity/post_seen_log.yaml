type: object
properties:
  userId:
    type: integer
  postId:
    type: integer
  user:
    $ref: 'user.yaml'
  createdAt:
    type: string
    format: date-time
required:
  - userId
  - postId
  - createdAt