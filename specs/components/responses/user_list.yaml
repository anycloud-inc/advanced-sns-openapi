description: OK
content:
  application/json:
    schema:
      type: object
      title: ResponseUser
      properties:
        users:
          type: array
          items:
            $ref: '../schemas/entity/user.yaml'
      required:
        - users
