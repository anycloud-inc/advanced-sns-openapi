title: RequestCreatePost
type: object
required:
  - post
properties:
  post:
    type: object
    properties:
      body:
        type: string
      viewableUserIds:
        type: array
        items:
          type: integer
    required:
      - body
